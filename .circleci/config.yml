version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.0
    steps:
      - checkout
      - run: echo "A first hello"
      - run:
          command: |
            python3 -m venv venv
            python test_hello.py
